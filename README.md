# Siberian Health Parser

Настраиваемый браузерный парсер каталога [ru.siberianhealth.com](https://ru.siberianhealth.com/ru/shop/catalog/) с зеркалированием иерархии в Google Drive и дедупликацией повторяющихся сертификатов.

## Что умеет

- **Иерархия:** `Категория → Подкатегория → Товар` — на Google Drive создаётся идентичная структура папок (`SiberianHealthParser/<категория>/<подкатегория>/<название товара>/`).
- **Карточка товара:**
  - автоматически кликает «Читать полное описание» и собирает полный текст;
  - кликает по вкладке **Состав** — копирует;
  - кликает по вкладке **Отзывы**, проходит по всем страницам пагинации, копирует все отзывы;
  - собирает все изображения карточки в папку `images/`;
  - складывает текстовые данные в `info.txt` внутри папки товара.
- **Документы:** скачивает PDF/JPG из раздела «Документы и материалы». Если один и тот же сертификат (`Халяль`, свидетельства о регистрации и т.п.) встречается у разных товаров — он загружается **один раз** в общую папку `_shared/certificates/`, а в папке каждого товара создаётся ярлык (Drive shortcut). Дедуп идёт и по URL, и по SHA-256 содержимого.
- **Настройка:** локальный веб-UI (открывается в твоём браузере) — выбираешь нужные категории галочками или вставляешь ссылку на отдельный товар. Можно поставить лимит товаров на категорию.
- **Возобновление:** все категории/товары/файлы пишутся в SQLite (`state.db`), так что прерванный запуск можно продолжить — уже загруженные товары/файлы парсер пропустит.

## Стек

- Python 3.11+
- [Playwright](https://playwright.dev/python/) (браузер Chromium) для парсинга
- [FastAPI](https://fastapi.tiangolo.com/) + WebSocket для UI и live-логов
- [google-api-python-client](https://github.com/googleapis/google-api-python-client) для Google Drive
- SQLite для состояния и дедупа

## Установка

```bash
git clone https://github.com/magnitos93/siberianhealth-parser.git
cd siberianhealth-parser

python -m venv .venv
source .venv/bin/activate   # Windows: .venv\Scripts\activate
pip install -e ".[dev]"
playwright install chromium
```

## Настройка Google Drive

1. Зайди в [Google Cloud Console](https://console.cloud.google.com/), создай (или выбери) проект.
2. Включи API: **APIs & Services → Library → Google Drive API → Enable**.
3. Создай OAuth-клиент: **APIs & Services → Credentials → Create Credentials → OAuth client ID**. Тип — **Desktop app**.
4. Скачай JSON и положи в корень проекта как `credentials.json` (или укажи путь в `.env`).
5. На странице **OAuth consent screen** добавь себя в Test users (если приложение в режиме Testing).

При первом запуске (нажатии «Войти в Google» в UI) откроется окно для согласия. После этого в проект сохранится `token.json`, и больше логин не понадобится.

> **Scope:** парсер запрашивает только `drive.file` — он видит и редактирует **только те файлы и папки, которые сам создал**. Доступа ко всему твоему Диску нет.

## Запуск

### Веб-UI (рекомендую)

**Windows — двойным кликом:** в папке проекта дважды щёлкни `run.bat`. Он сам активирует окружение и запустит сервер. Откроется окно cmd, дальше открой http://127.0.0.1:8765/ в браузере.

**Из терминала (Win/Mac/Linux):**

```bash
sibparser serve
# или:  python -m sibparser serve
```

UI откроется в браузере по адресу `http://127.0.0.1:8765/`. Дальше:

1. **«Войти в Google»** — авторизация Drive.
2. **«Открыть каталог»** — Playwright откроет сайт, прочитает меню и покажет дерево категорий.
3. Расставь галочки на нужных категориях (или вставь URL одного товара в правое поле «Один товар»).
4. **«Запустить выбранные категории»** — лог пишется live в WebSocket.

### Из командной строки

```bash
# Распарсить один товар (без Drive)
sibparser run --product https://ru.siberianhealth.com/ru/shop/catalog/product/400273/ --no-upload

# Распарсить категорию с лимитом 5 товаров
sibparser run -c "Каталог/Чаи и напитки" --limit 5

# Просто посмотреть дерево
sibparser discover
```

## Конфигурация

Все настройки читаются из `.env` (см. `.env.example`):

| Переменная | По умолчанию | Что это |
|---|---|---|
| `SIBPARSER_GOOGLE_CREDENTIALS` | `./credentials.json` | OAuth client secret |
| `SIBPARSER_GOOGLE_TOKEN` | `./token.json` | Кеш refresh-токена |
| `SIBPARSER_DRIVE_ROOT_FOLDER` | `SiberianHealthParser` | Имя корневой папки на Диске |
| `SIBPARSER_DOWNLOADS_DIR` | `./downloads` | Локальная папка для `--no-upload` режима |
| `SIBPARSER_STATE_DB` | `./state.db` | Файл SQLite |
| `SIBPARSER_HEADFUL` | `true` | Показывать окно браузера или нет |
| `SIBPARSER_REQUEST_DELAY` | `0.5` | Пауза между страницами, сек |
| `SIBPARSER_HOST` / `SIBPARSER_PORT` | `127.0.0.1` / `8765` | Адрес локального UI |

## Структура папки на Drive

```
SiberianHealthParser/
├── _shared/
│   └── certificates/
│       ├── 3689.jpg                  ← один реальный файл
│       └── halal-2024.pdf
├── Витамины и добавки/
│   └── Все минералы/
│       ├── Кальций активный [123456]/
│       │   ├── info.txt
│       │   ├── images/
│       │   │   ├── 123456_a1b2.jpg
│       │   │   └── 123456_c3d4.jpg
│       │   └── documents/
│       │       ├── Свидетельство о регистрации   ← shortcut на _shared/certificates/3689.jpg
│       │       └── Каталог Кальций 2024.pdf
│       └── ...
└── ...
```

## Структура `info.txt`

```
Название: Кальций активный
URL: https://...
ID товара: 123456
Артикул: 123456
Серия: ...
Объём: ...
Цена: ...

============================================================
ОПИСАНИЕ
============================================================
...полный текст после клика «Читать полное описание»...

============================================================
СОСТАВ
============================================================
...

============================================================
ОТЗЫВЫ (32)
============================================================
--- Отзыв #1 Анна | 2024-08-12 | 5 ---
...

============================================================
ДОКУМЕНТЫ И МАТЕРИАЛЫ
============================================================
- Свидетельство о государственной регистрации: https://...
```

## Тесты и линт

```bash
ruff check src tests
mypy src
pytest -q
```

Тесты используют сохранённый снапшот страницы товара (`scripts/_explore/sample_product.html`) и не требуют сети.

## Разработка

```bash
# Проверить site-парсер на живом сайте (не трогая Drive)
python scripts/explore_site.py     # пере-снять снапшот в scripts/_explore/

# UI-режим: открыть Playwright-окно для отладки
SIBPARSER_HEADFUL=true sibparser serve
```

## Лицензия

MIT.
